[user]
	name = Alexander Isaev
	email = IsaevAV27@gmail.com
[core]
	autocrlf = input
	editor = vim
[diff]
	tool = vimdiff
[alias]
    co = checkout
    ci = commit
    st = status
    br = branch
	hist = "log --pretty=format:'%C(yellow)[%ad]%C(reset) %C(green)[%h]%C(reset) | %C(red)%s %C(bold red){{%an}}%C(reset) %C(blue)%d%C(reset)' --graph --date=short"
    type = cat-file -t
    dump = cat-file -p


